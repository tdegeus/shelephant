version: 2

sphinx:
  configuration: docs/conf.py

build:
  os: ubuntu-latest-lts
  tools:
    python: mambaforge-22.9

conda:
  environment: environment.yaml

python:
  install:
  - method: pip
    path: .
